using System;
public class Word
{
    private string text;
    private bool isVisible;
    public string Text { get {retun text; }}
    public bool IsVisible { get {retun isVisible; }}
    public Word(string text)
    {
        this.text = text;
        this.isVisible = IsVisible;
    }
}